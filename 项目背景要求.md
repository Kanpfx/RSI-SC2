# SC2-RSI 第一版架构

## 1. 核心

目标：实现一个**最小可工作的 StarCraft II Bot 自进化系统**。

```text
Bot
↓
10 局固定评测
↓
记录结果
↓
RSI 分析问题
↓
生成多个 Candidate
↓
RSI 使用工具修改 bot/
↓
Smoke Test
↓
Git Commit
↓
每个 Candidate 再跑 10 局
↓
Archive + Tree Search 选择下一批 Parent
↓
继续进化
```

第一版原则：

- RSI 本身固定，只允许修改 `bot/`
- Seed Bot 尽量基础，给进化留空间
- RSI 必须是真实可工作的 Tool Agent
- 搜索采用简化的 **DGM-style Archive + Beam Search**
- 版本控制全部使用 Git
- 暂不做 Memory、Multi-Agent、RAG、模型训练

---

## 2. 文件结构

```text
sc2-rsi/
│
├── bot/                         # 唯一可进化区域
│   ├── main.py
│   └── modules/
│       ├── observation/
│       │   └── state_snapshot.py
│       ├── economy/
│       │   └── macro_manager.py
│       ├── combat/
│       │   └── army_controller.py
│       └── strategy/
│           └── strategy_config.py
│
├── rsi/                         # 第一版固定
│   ├── loop.py
│   │
│   ├── agent/
│   │   ├── runner.py
│   │   └── context.py
│   │
│   ├── llm/
│   │   ├── base.py
│   │   └── client.py
│   │
│   ├── tools/
│   │   ├── edit.py
│   │   ├── bash.py
│   │   └── git.py
│   │
│   ├── analysis/
│   │   └── analyzer.py
│   │
│   ├── evaluation/
│   │   ├── runner.py
│   │   ├── metadata.py
│   │   └── selector.py
│   │
│   └── evolution/
│       ├── archive.py
│       ├── search.py
│       └── state.py
│
├── prompts/
│   ├── analyze.md
│   └── improve.md
│
├── config/
│   └── mvp.yaml
│
├── tests/
├── runs/
├── README.md
└── architecture.md
```

适合扩展的能力使用目录，但第一版每个目录内部保持最小实现。

---

## 3. Seed Bot

基于 `python-sc2 / burnysc2`。

### `main.py`

负责：

- BotAI 生命周期
- 调用 economy / combat / observation
- 启动游戏

### `observation/state_snapshot.py`

读取基础状态：

```text
minerals
supply
workers
army
game_time
```

### `economy/macro_manager.py`

第一版只实现：

```text
持续生产 SCV
distribute workers
Supply 不足时建 Supply Depot
建 1 个 Barracks
持续生产 Marine
```

暂不实现复杂科技树、扩张、升级和 Build Order。

### `combat/army_controller.py`

第一版只实现：

```text
Marine 数量达到阈值
→ attack enemy start location
```

不做复杂微操。

### `strategy/strategy_config.py`

保存少量固定参数，例如：

```python
attack_threshold = 12
```

Seed Bot 只需要**能稳定完成一局，但明显有改进空间**。

---

## 4. RSI Agent

实现最小 Tool-calling Loop：

```text
Prompt
→ LLM
→ Tool Call
→ Tool Result
→ LLM
→ ...
→ Finish
```

`agent/runner.py` 负责：

- 调用 LLM
- 执行 Tool Call
- 回传 Tool Result
- 控制最大步骤数

第一版不做：

```text
Multi-Agent
Planner
Sub-Agent
Tree-of-Thought
长期上下文 Memory
```

`llm/` 提供统一接口，模型配置从环境变量读取：

```text
LLM_API_KEY
LLM_BASE_URL
LLM_MODEL
```

---

## 5. Tools

第一版只保留三个 Tool，参考 DGM 的最小自修改思路。

### `edit.py`

负责代码读取与修改：

```text
view_file
search_text
replace_text
write_file
```

约束：

```text
写操作只允许 bot/**
```

第一版使用普通文本编辑即可，不做 AST。

### `bash.py`

负责：

```text
运行 Python
pytest
compileall
ripgrep
其他基础命令
```

需要：

```text
stdout
stderr
exit code
timeout
```

### `git.py`

负责：

```text
status
diff
current_commit
create_branch
checkout
add
commit
merge --ff-only
changed_files
```

Agent 可以查看：

```text
git status
git diff
```

branch / commit / merge 由 Evolution Loop 控制。

---

## 6. Analyzer / Improver

### Analyzer

输入：

```text
当前 Parent Bot
最近 10 局结果
该节点历史修改信息
```

输出：

```text
一个主要问题
+
若干可独立尝试的改进方向
```

例如：

```text
Problem:
经济发展过慢

Candidates:
A. 提前建 Barracks
B. 调整 SCV 持续生产逻辑
```

### Improver

对每个候选方向分别运行一次 Agent：

```text
Parent
├── Candidate A
└── Candidate B
```

要求：

- 每个 Candidate 只尝试一个主要修改
- 只修改 `bot/**`
- 修改后运行基础测试
- 不允许修改 RSI / Benchmark / Config / Tests

---

## 7. 搜索策略

第一版不使用单链进化，采用简化的：

```text
Archive + Beam Search
```

结构类似：

```text
             Seed
           /      \
         A          B
       /  \        /  \
     A1   A2     B1   B2
```

### 最小算法

默认参数：

```yaml
search:
  beam_width: 2
  branch_factor: 2
```

含义：

```text
beam_width = 每轮最多选择 2 个 Parent
branch_factor = 每个 Parent 生成 2 个 Candidate
```

每轮：

```text
1. 从 Archive 中选当前最好的最多 2 个可扩展节点
2. 每个 Parent 生成 2 个不同 Candidate
3. 每个 Candidate：
   - 修改
   - Smoke Test
   - Git Commit
   - 跑 10 局
4. 所有结果进入 Archive
5. 按 wins 排序
6. 选前 2 个作为下一轮 Frontier
```

第一版排序：

```text
wins 高优先
crash > 0 淘汰
wins 相同时优先更浅 / 更早节点
```

Candidate 不要求一定优于 Parent 才进入 Archive，但只有表现靠前的节点会继续扩展。

这样保留：

```text
多分支
回溯可能
历史节点
不同进化路径
```

但仍然足够简单。

---

## 8. Archive

`evolution/archive.py` 保存所有评测节点。

每个节点记录：

```json
{
  "id": "gen2_b",
  "parent_id": "gen1_a",
  "commit": "...",
  "generation": 2,

  "wins": 6,
  "losses": 4,
  "ties": 0,
  "crashes": 0,

  "expanded": false
}
```

Archive 主要用于：

```text
保存搜索树
选择 Parent
追踪 lineage
避免只保留单一路径
```

版本本体仍然由 Git 管理。

---

## 9. Git 版本方式

主要分支：

```text
main
└── RSI 框架

candidate/<node_id>
└── 每个搜索节点对应一个 Candidate commit
```

每个 Candidate：

```text
Parent Commit
↓
创建 candidate/<id>
↓
RSI 修改 bot/
↓
Smoke Test
↓
Commit
↓
10 局评测
↓
加入 Archive
```

第一版不需要把所有优秀版本 merge 到一条 `evolution` 分支。

Archive 中直接保存：

```text
node_id → git commit
```

搜索时从目标 Parent commit 创建新 Candidate 即可。

---

## 10. Evaluation

第一版固定：

```text
10 局
非实时
固定地图
Terran vs Terran
AI Difficulty = Lv7 / VeryHard
AI Build = Random
```

配置示例：

```yaml
evaluation:
  games: 10
  realtime: false
  map: AcropolisLE

  bot_race: Terran

  opponent:
    race: Terran
    difficulty: VeryHard
    build: RandomBuild
```

如果本机没有该地图，只修改配置为一个已有地图；同一次实验保持固定。

---

## 11. Metadata

第一版只记录必要信息：

```json
{
  "candidate_id": "gen2_b",
  "parent_id": "gen1_a",
  "commit": "...",

  "games": 10,
  "wins": 6,
  "losses": 4,
  "ties": 0,
  "crashes": 0
}
```

每局记录：

```text
result
duration
crashed
error
```

暂不记录复杂 Replay、APM、经济曲线等信息。

---

## 12. Smoke Test

Candidate 进入 SC2 评测前运行：

```text
python -m compileall bot
pytest
import bot.main
检查 Git changed files
```

如果：

```text
测试失败
修改了 bot/ 外文件
```

直接标记 Candidate 无效。

---

## 13. Evolution Loop

`rsi/loop.py` 负责：

```text
1. 初始化 Seed
2. Seed 跑 10 局并进入 Archive
3. Search 选择最多 beam_width 个 Parent
4. Analyzer 为每个 Parent 生成改进方向
5. 每个 Parent 创建 branch_factor 个 Candidate
6. RSI Agent 修改 bot/
7. Smoke Test
8. Git Commit
9. 每个 Candidate 跑 10 局
10. 保存 Metadata
11. 写入 Archive
12. Search 选择下一轮 Frontier
13. 重复
```

---

## 14. Config

```yaml
project:
  max_generations: 5

agent:
  max_steps: 20

search:
  beam_width: 2
  branch_factor: 2

evaluation:
  games: 10
  realtime: false
  map: AcropolisLE

  bot_race: Terran

  opponent:
    race: Terran
    difficulty: VeryHard
    build: RandomBuild

tools:
  bash_timeout_sec: 60
```

---

## 15. 第一版不做

```text
Long-term Memory
RAG
Replay 深度分析
MCTS
复杂 Evolutionary Algorithm
Novelty Search
Multi-Agent
Tool 自动生成
Prompt 自进化
RSI 自修改
Evaluator 自修改
模型训练
```

搜索先保持：

```text
DGM-style Archive
+
简单 Beam / Tree Search
```

---

## 16. Codex 实现顺序

```text
1. 项目目录 + Config
2. Seed Bot
3. 10 局 Evaluation
4. Metadata
5. edit / bash / git Tools
6. LLM Client + Agent Runner
7. Analyzer + Improver
8. Git Candidate 创建
9. Archive
10. Beam Search
11. Evolution Loop
12. Tests + README
```

---

## 17. Definition of Done

执行：

```bash
python -m rsi.loop --config config/mvp.yaml
```

能够自动完成：

```text
Seed 评测
→ 选择 Parent
→ 生成多个 Candidate
→ RSI 使用工具修改 Bot
→ Smoke Test
→ Git Commit
→ 每个 Candidate 跑 10 局
→ 写入 Archive
→ Beam Search 选择下一批 Parent
→ 继续下一代
```

这就是第一版完成标准。
